import 'package:flutter/material.dart';

class FanSponsor extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          child: Text('Sponsor Screen'),
        ),
      ),
    );
  }
}
